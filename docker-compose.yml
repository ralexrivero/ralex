services:
  ralex-works:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ralex-works
    ports:
      - "80:80"
    networks:
      - web
    restart: unless-stopped

networks:
  web:
    external: true
